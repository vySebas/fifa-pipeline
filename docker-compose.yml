services:
  pipeline:
    build: .
    ports:
      - "8050:8050"
    volumes:
      - .:/workspace
    working_dir: /workspace
    command: sh -c "python3 flows/ingest.py && dbt run && dbt test && python dashboard.py"
